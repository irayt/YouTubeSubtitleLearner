# Copy this file to .env and fill the values

# Supabase
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY

# STT engines
# Set one:
#  - STT_ENGINE=openai          # use OpenAI Whisper API (requires OPENAI_API_KEY)
#  - STT_ENGINE=faster_whisper  # use local faster-whisper (no external API)
STT_ENGINE=faster_whisper

# Translation engine
#  - TRANSLATE_ENGINE=argos     # offline (default)
#  - TRANSLATE_ENGINE=openai    # OpenAI Chat translate
TRANSLATE_ENGINE=argos

# OpenAI (used when STT_ENGINE=openai or TRANSLATE_ENGINE=openai)
#OPENAI_API_KEY=sk-...

# Worker behavior
# 1 = disable STT and require public CC; 0 = allow STT fallback
DISABLE_STT=1
# Transcribe only the first N seconds (when STT enabled)
MAX_AUDIO_SECONDS=90

# Single-shot mode (process exactly one video and exit)
# Set both to enable single-shot; leave empty for queue mode
VIDEO_ID=
LANG=en

# Translation options (applies to STT outputs)
TRANSLATE_ENABLED=1
STORE_SOURCE_LANG=1
ARGOS_AUTO_DOWNLOAD=1   # auto download language packs when needed

# faster-whisper options (when STT_ENGINE=faster_whisper)
# FW_MODEL can be: tiny, base, small, medium, large-v3 (downloads on first run)
FW_MODEL=small
FW_DEVICE=cpu
FW_COMPUTE_TYPE=int8  # int8 / int8_float16 / float16 / float32
